import React from "react";

type Props = {
  gameRoom: GameRoomType;
  nick: string;
  gameDirectorCB: Function;
};

export const MultiGameResult = ({ gameRoom, nick, gameDirectorCB }: Props) => {
  return <div>Results bitches</div>;
};
